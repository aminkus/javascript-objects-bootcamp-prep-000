var playlist = {artistName: 'songTitles'};
function updatePlaylist(object, name, song)
function removeFromPlaylist(playlist,artistName){object[name] = title; return playl
  delete playlist.Slowdive; return playlist
}